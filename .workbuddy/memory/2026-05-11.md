# 2026-05-11

## 项目：兰博的设计小摊 - 网站开发

### 今日工作

**作品集页面（portfolio.html）修改：**
- 作品标题改为最多显示1行（`-webkit-line-clamp: 1`）
- 作品描述改为最多显示2行（`-webkit-line-clamp: 2`）
- 喜欢程度星星改为：单个星星图标 + 数字（去掉了5颗星星的显示）
- 排序按钮样式重构：
  - 去掉了左边图标
  - 文字改为"发布时间"/"喜欢程度"
  - 右边改为上下双箭头（一亮一灰表示当前排序方向）
- 排序切换逻辑：desc（新→旧/多→少）时下箭头亮，asc（旧→新/少→多）时上箭头亮

**首页（index.html）修改：**
- `.work-card__desc` 添加最多2行截断
- `.article-card__desc` 添加最多2行截断

### 项目文件结构
- `/index.html` - 首页
- `/portfolio.html` - 作品集页面
- `/about.html` - 关于页面
- `/images/` - 图片资源目录

## 2026-05-12 补充

### 首页修改
1. "查看更多作品"按钮改为链接到 portfolio.html
2. "查看更多文章"按钮改为链接到 article.html
3. 首页最新文章列表：
   - 添加封面图片
   - 去掉时间显示（date-box）
   - 改为横向布局（封面 + 文字内容）

### 新增页面
1. **work-detail.html** - 作品详情页
   - 显示作品标题、描述、标签
   - 底部显示多张作品图片，竖向排列
   - 通过 URL 参数 `?id=X` 加载对应作品数据
   - 首页和作品集页面的作品卡片可点击跳转

2. **article-detail.html** - 文章详情页
   - 显示文章标题、标签、发布日期、阅读数
   - 图文并茂的文章内容
   - 通过 URL 参数 `?id=X` 加载对应文章数据
   - 首页和文章页面的文章卡片可点击跳转

### 修改的文件
- `/index.html` - 添加卡片点击事件、修改文章卡片样式
- `/portfolio.html` - 添加作品卡片点击事件
- `/article.html` - 添加文章卡片点击事件
- `/work-detail.html` - 新增
- `/article-detail.html` - 新增

## 2026-05-13 补充

### 作品详情页（work-detail.html）布局修改
1. **深蓝色卡片区域调整：**
   - 去掉与顶部导航栏的边距（紧贴导航栏）
   - 深蓝色区域占满全屏宽度，没有左右边距
   - 添加内层容器 `.work-detail-card__inner` 限制最大宽度 1280px

2. **返回按钮调整：**
   - 将"返回作品集"按钮移到深蓝色卡片内标题左上方
   - 按钮文字改为"返回"
   - 调整视觉样式（毛玻璃效果、深蓝色背景适配）
   - 设置 `align-self: flex-start` 实现左对齐

3. **内容对齐方式：**
   - 返回按钮左对齐
   - 标题和元数据居中
   - 深蓝色区域与下方白色区域的间距调整

4. **左对齐修复：**
   - 修改 `.detail-container` 的 padding 从 `32px 0 60px 0` 改为 `32px 48px 60px 48px`
   - 使返回按钮与下方白色底块（.work-gallery-card）左对齐

### 修改的文件
- `/work-detail.html` - 布局样式多次调整

## 2026-05-13 补充（2）

### 文章详情页（article-detail.html）布局修改
1. **内容区域宽度调整：**
   - 从 780px 改为 1280px（与首页和作品详情页一致）
   - 容器最大宽度：`max-width: 1280px`

2. **缩小竖向区块间距：**
   - `.article-container` padding：60px 48px 80px → 32px 48px 60px
   - `.back-btn` margin-bottom：40px → 24px
   - `.article-header` padding：48px → 40px，margin-bottom：32px → 24px
   - `.article-body` padding：48px → 40px
   - `.article-body h2` margin：40px 0 20px → 32px 0 16px
   - `.article-body p` margin-bottom：20px → 16px
   - `.article-body ul/ol` margin：20px 0 → 16px 0
   - `.article-body li` margin-bottom：12px → 10px
   - `.article-body blockquote` margin：32px 0 → 24px 0，padding：24px 32px → 20px 28px
   - `.article-body img` margin：32px 0 → 24px 0
   - `.article-body .highlight-box` margin：32px 0 → 24px 0，padding：24px → 20px
   - `.article-body .image-grid` margin：32px 0 → 24px 0
   - `.article-body .code-block` margin：32px 0 → 24px 0，padding：24px → 20px

### 修改的文件
- `/article-detail.html` - 布局样式调整

## 2026-05-13 补充（3）

### 全站链接修复
- `index.html`：Logo 链接 `#` → `index.html`，移动端菜单"首页"和"关于我"链接 `#` → 正确链接
- `about.html`：PC菜单和移动端菜单"作品集"链接 `#` → `portfolio.html`

### 全站 Footer Logo 替换
- 所有 7 个 HTML 文件的底部 `.footer__logo` 中的文字 `L` 替换为 `<img src="images/logo-white@1x.png">`
- 涉及文件：`index.html`、`portfolio.html`、`about.html`、`article.html`、`contact.html`、`work-detail.html`、`article-detail.html`

### 修改的文件
- `/index.html` - 修复链接 + 替换 footer logo
- `/about.html` - 修复链接 + 替换 footer logo
- `/portfolio.html` - 替换 footer logo
- `/article.html` - 替换 footer logo
- `/contact.html` - 替换 footer logo
- `/work-detail.html` - 替换 footer logo
- `/article-detail.html` - 替换 footer logo
